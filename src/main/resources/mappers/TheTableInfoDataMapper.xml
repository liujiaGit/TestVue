<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.testvue.mapper.TheTableInfoDataMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.testvue.entity.TheTableInfoData">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="info_data" property="infoData" />
        <result column="rate" property="rate" />
        <result column="img" property="img" />
    </resultMap>
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, info_data, rate, img
    </sql>

    <select id="selectList" resultType="com.example.testvue.entity.TheTableInfoData">
        select * from The_Table_Info_Data;
    </select>

</mapper>
